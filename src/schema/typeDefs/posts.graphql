type Post {
  id: ID!
  text: String!
  user: [User]
}

type Query {
  post(id: ID!): Post
  allPosts: [Post] @isAuthenticated
}

type Mutation {
  addPost(text: String!): Post @hasScope(scope: ["add:post"])
}
